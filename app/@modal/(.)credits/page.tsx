import CreditsCard from "@/app/credits/CreditsCard";
import CreditsModal from "@/app/ui/CreditsModal";

export default function CreditsModalPage() {
  return (
    <CreditsModal>
      <CreditsCard />
    </CreditsModal>
  );
}
